#!/bin/bash
# Traffic Keeper - 升级后回调脚本
# 清理缓存并重启应用，确保加载最新代码

APP_DIR="${TRIM_APPDEST}"

# 清理 Python 缓存
find "${APP_DIR}" -name "*.pyc" -delete 2>/dev/null || true
find "${APP_DIR}" -type d -name "__pycache__" -exec rm -rf {} + 2>/dev/null || true

# 重启应用（应用中心会调用 main start，但这里确保清理）
exit 0